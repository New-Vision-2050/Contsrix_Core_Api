# Tenant Configuration

# Database connection for tenants
TENANT_DB_CONNECTION=pgsql

# Default schema to use when no tenant is set
TENANT_DEFAULT_SCHEMA=public

# Domain configuration
TENANT_USE_DOMAINS=false
TENANT_CENTRAL_DOMAIN=app.example.com
TENANT_DOMAIN_FORMAT={tenant}.example.com